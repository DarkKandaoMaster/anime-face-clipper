# 前端开发交接

给 `headcount-30s` 做一个本机 Web 界面：**上传视频 → 分析 → 结果页拖 X 滑块实时看片段 → 打包下载**。
本文只写这次新增的东西。流水线本身、素材、阈值、Windows 中文路径那些坑见 [CLAUDE.md](./CLAUDE.md)，不重复。

---

## 1. 目录

```
backend/          FastAPI（目录已建，代码待写）
  routers/
frontend/         Nuxt 4.5.2，已建好并跑通，见第 7 节
design/           设计画板，见第 7 节
src/              已有流水线。可以改，但改完 pytest 必须全绿
storage/          运行时数据，已 gitignore
```

---

## 2. 已定的决策（附理由，不要推翻）

| 决策 | 理由 |
|---|---|
| **运行前零参数**，唯一可调的是 X，且只出现在结果页 | 用户对 X 取值没有直觉，事前猜不如事后调；其余参数（`identity_threshold`、`min_face_height_ratio`）跨画风不可比，暴露出去必出事 |
| **不做用户系统**，只有一个共享访问码 | 公网上传 + GPU + 下载，无门槛等于开放转码服务器。一个 env 变量 + 一个依赖项，约 20 行 |
| **不用 Redis** | 本机 1 张显卡 → 并行度 1；Web 是单个 uvicorn 进程 → 进度用 `multiprocessing.Manager().dict()` 就够。Redis 留给上公网那一步（Linux，一行装完） |
| **MySQL 只存任务元数据**，二进制全在文件系统 | 大文件进数据库要整个读进内存，且没法 HTTP Range 断点续传 |
| **上传与分析是两个阶段**，视频必须完整落盘才能开跑 | `cv2.VideoCapture` / `ffmpeg scdet` / `ffmpeg -ss` 都要一个能随机定位的文件路径，吃不了字节流。所谓「流式不落盘」指的是**解码出来的帧**不落盘，不是视频文件 |
| **分析时不编码片段**（等价 `--no-clip`） | X 还没定，编出来的一大半会被拖走 |
| **预览不编码**：`<video>` + HTTP Range 播原视频，`currentTime` 跳到片段起点，播满 30 秒停 | 零编码零额外磁盘。只在点「下载」时才调 `_encode_clip` 现场生成 |
| **假定上传全是 H.264 / MP4** | 上传完 `ffprobe` 探一次，不是就 fail fast 报错，不做转码。以后要支持 HEVC/mkv，在这个探测点加分支 |
| **并行度 = 显卡数** | README 第五轮实测：同一张卡上多进程跑多个视频 145s vs 147s，瓶颈全在 GPU |

---

## 3. 环境（2026-08-21 实测，已就绪）

- Node **22.14.0** / npm 10.9.2 / **pnpm 10.33.0**
- MySQL **8.0.24**，库 `headcount30s` 已建（`utf8mb4` / `utf8mb4_0900_ai_ci`）
- 端口 3000 / 8000 空闲，3306 是 MySQL
- Nuxt 4.5.2 已装在 `frontend/`（见第 7 节），`pnpm dev` 实测能起、页面 200
- Python 包已齐：`fastapi` `uvicorn` `starlette` `sqlalchemy` `pydantic` `python-multipart` `httpx` `cryptography` `pymysql` `aiofiles` `watchfiles`
- GPU：**1 张** RTX 4060 Laptop 8 GB
- 磁盘：D 盘剩约 30 GB。存储放 `storage/`，塞得下十来部番，不够长期用

**凭据全部走环境变量**，已写在项目根的 `.env`（已 gitignore）：
`MYSQL_HOST` `MYSQL_PORT` `MYSQL_USER` `MYSQL_PASSWORD` `MYSQL_DB` `STORAGE_ROOT` `APP_ACCESS_CODE` `PYTHON_BIN` `GPU_COUNT`

缺任何一个直接 `RuntimeError` 退出，不要写默认值兜底，不要打印到日志。

---

## 4. 存储布局

```
storage/
  assets/{asset_id}/source.mp4        上传的原视频
                   /poster.jpg        ffprobe 抽的封面
                   /meta.json         时长/分辨率/编码/体积
  tasks/{task_id}/{video_stem}/       直接用 run_pipeline 的输出目录
                   windows.json
                   windows_scan.json  ← 第 6 节新增
                   tracks.json
                   crops/
                   clips/             按需生成，可随时删
```

---

## 5. 数据库表

```
assets      id, filename, path, duration, width, height, codec, size_bytes, created_at
tasks       id, status, created_at, started_at, finished_at, error
task_items  id, task_id, asset_id, status, style, num_tracks, num_characters, out_dir, error
```

**不建片段表**。片段随 X 变化，是算出来的，不是存下来的。

---

## 6. 必须做的 `src/` 改动

`process_video` 跑完时，除现有的 `windows.json` 外**再落一份 `windows_scan.json`**：

```json
{
  "duration": 1432.5,
  "frame_interval": 0.3,
  "window_seconds": 30,
  "cuts": [12.4, 31.9, ...],
  "windows": [{"t": 0.0, "n": 3}, {"t": 0.3, "n": 3}, ...]
}
```

`n` = 该起点窗口内的角色数（计数口径同 `_window_characters`，即 `cluster_scope="window"` 下窗口内重聚类的结果）。

**为什么**：README 已写明「改 X 只需 `--min-events X`，不用重跑检测或聚类」。落下这份中间态，`GET /tasks/{id}/segments?x=N` 就退化成一次纯 Python 比较，几十毫秒返回，滑块才拖得动；否则改一个数字要等 6 分钟。

**做法**：照抄 [sweep.py](src/sweep.py) 的切分——扫一次视频、提一次特征，后面只重跑选段。

**后端的选段逻辑照抄 [`select_segments`](src/main.py)**：命中 → 起点吸附到 `clip_snap_max_shift` 内最近的切镜点 → 吸附后复核仍达标才采用 → `k` 跳到 `start + window` 之后。不要自己另发明一套，两边会走偏。

**改完必须跑 `pytest tests -q`，141 个用例全绿。** 顺手给新逻辑补几个纯逻辑单测（`tests/` 全用合成数据，不需要 GPU/模型/网络）。

---

## 7. 前端

**工程已经建好了，不要重建。** `frontend/` 里是 Nuxt **4.5.2**（`app/` 目录结构），已完成：

- 模块只装了 `@nuxt/fonts`——它在构建时把 IBM Plex Sans / Mono 下载并自托管，运行时不请求 Google，离线可跑
- `app/assets/css/tokens.css`：设计令牌的唯一来源，已在 `nuxt.config.ts` 里全局引入
- `nitro.devProxy` 把 `/api` 转到 `http://127.0.0.1:8000/api`，前端一律用相对路径请求，不要写死端口
- `devServer.host = '127.0.0.1'`：**不设这个的话 Nuxt dev 只绑 `::1`，`curl 127.0.0.1:3000` 会连不上**，别在这上面耗时间
- `app/pages/index.vue` 是占位页，翻画板时直接替换

**不要引 Tailwind。** 画板全是内联样式 + CSS 变量，值都很碎（`12.5px`、`9.5px`、`letter-spacing: 0.16em`、`rgb(232 163 61 / 0.13)`），进了 Tailwind 全得写成方括号任意值，绕一圈回到原地还多一层构建。用 `tokens.css` + SFC 里的 `<style scoped>`。

设计源在 `design/`，是**真的 HTML + CSS**，所有数值都在源码里，别从截图猜：

- [design/Main.dc.html](design/Main.dc.html) — 工作台
- [design/Dashboard.dc.html](design/Dashboard.dc.html) — 结果页

`.dc.html` 是设计画布自己的格式，翻成 Vue 的对照：

| 画板 | Vue |
|---|---|
| `<helmet><style>` 里的 `:root{...}` | 抽成 `app/assets/css/tokens.css` 全局引入 |
| `<sc-for list="{{groups}}" as="g">` | `v-for="g in groups"` |
| `{{sel.tc}}` | 一样 |
| `onClick="{{c.pick}}"` | `@click="pick(c)"` |
| `<sc-if value="{{isEmpty}}">` | `v-if="isEmpty"` |
| 底部 `class Component extends DCLogic` 的 `renderVals()` | `computed` 或 composable |
| `<x-dc>`、`<script src="./support.js">` | 删掉，是画布的壳 |
| 元素上的 `style="..."` | 原样保留 |

**画板里的数据全是假的**：人脸和缩略图是占位图形，曲线是按实测分布造的样本。真实数据来自 `windows_scan.json` 和 `crops/`。画板上画了 GPU 0 / GPU 1 两块卡，本机只有一张，按 `GPU_COUNT` 渲染。

两个页面：`/`（工作台）和 `/results/[taskId]`（结果页）。

---

## 8. 接口

| 操作 | 接口 |
|---|---|
| 校验访问码 | `POST /api/auth/verify` |
| 分片上传 | `POST /api/uploads/init` → `POST /api/uploads/{id}/chunk` ×N → `POST /api/uploads/{id}/complete` |
| 素材库 | `GET /api/assets` / `DELETE /api/assets/{id}` |
| 封面 | `GET /api/assets/{id}/poster` |
| 建任务 | `POST /api/tasks`（传 asset_ids） |
| 进度 | `GET /api/tasks/{id}/events`（SSE） |
| 取消 | `POST /api/tasks/{id}/cancel` |
| **拖 X 重算** | `GET /api/tasks/{id}/segments?x=6` |
| 播原视频（预览用） | `GET /api/assets/{id}/stream`（**必须支持 Range**） |
| 代表人脸 | `GET /api/tasks/{id}/{stem}/crops/{character_id}` |
| 下载单个片段 | `GET /api/tasks/{id}/clip?stem=..&start=..`（现场编码） |
| 打包下载 | `GET /api/tasks/{id}/download?x=6`（`StreamingResponse` 边压边发，压缩级别 0） |

要点：
- 分片 5–10 MB。整个 POST 一个大文件会撞反向代理的 `client_max_body_size`（Nginx 默认 1 MB），报 413 且很难查。
- 下载一律走字节流，**任何环节都不要 base64**：体积 +33%，且必须整个读进内存。
- 长任务绝对不能在请求处理函数里跑。uvicorn 是单线程异步的，6 分钟的 GPU 活会把事件循环卡死，期间连查进度都查不了。扔进 `ProcessPoolExecutor`，`max_workers = GPU_COUNT`，每个进程用 `CUDA_VISIBLE_DEVICES` 绑一张卡。

---

## 9. 验收标准

1. `uvicorn` 和 `pnpm dev` 都起得来，访问码拦得住未授权请求。
2. 传一段 `data/` 里的 30 秒素材 → 点开始分析 → 进度条走完 → 结果页出片段 → 拖 X 片段数跟着变 → 下载 zip 能正常打开播放。
3. `pytest tests -q` 全绿。
4. **中文文件名全程不乱码**：素材名、输出目录、zip 里的条目名。这是本项目最容易踩的坑。
5. 传一个非 H.264 或非 MP4 的文件，报出清楚的错误而不是静默失败。

---

## 10. 明确不做

用户系统、Redis、多机部署、转码、移动端适配、前端单元测试、画风选项（画风只作为分析结果展示，不给控件）。
