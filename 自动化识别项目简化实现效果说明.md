# 关于"自动化识别视频内是否存在满足 X 个主体的半分钟片段"项目简化实现的效果说明

核心结论：本项目是《自动化识别项目不可行性说明》中所述需求的简化实现，技术路线由行人重识别改为人脸检测加人脸/角色身份特征，并在原五环链路的每一环上补入了针对性措施。在 9 段各 30 秒、带人工标注的素材上，跨镜头去重后的角色数宏平均召回 0.98、宏平均 F1 0.95，画风路由 9 段全对，5 段角色数与真值完全一致；但压到 17 段完整片源（共 7.3 小时）后，同一套参数即失效：76% 的片长被判为合格片段，单个角色被拆成 4～5 个身份。作为改进手段实测的"正脸判定"未能成立，两种用法一为中性、一为有害，其制约因素在身份特征本身而非样本姿态。综上，简化实现在半分钟尺度上可用，在片长尺度上尚不可交付。

## 一、与原链路的对应关系

原说明所述五环链路保持不变，本项目在其中四环各加入一项措施，另新增一道前置的画风路由：

```
  画风路由 ──→ 人物检测 ──→ 镜头内跟踪/关联 ──→ 跨镜头身份去重 ──→ 滑窗内唯一身份计数 ──→ 存在性判断
  （新增）         │              │                   │                    │                  │
      │        人脸检测        ffmpeg 切镜         代表图 + 完全连接      30 秒定长窗口       仅输出"未发现"
      │        替代人体        + IoU 跟踪          层次聚类               计数达 X 即合格     不主张"不存在"
      │        + 显著度门槛    + 在线擂台选代表图
      │
      └─ 抽帧三分类 2D / 3D CG / 真人，一次判定，成套切换后续各环的模型、阈值与门槛
```

表 1　各环节的改动与动机

| 链路环节 | 本项目做法 | 针对原说明中的哪一条障碍 |
|---|---|---|
| 前置 | 画风三分类路由，按 2D / 3D CG / 真人整套切换检测器、身份特征、阈值、裁剪外扩与尺寸门槛 | 模型无法跨风格泛化 |
| 人物检测 | 改检人脸而非人体；2D 用动漫人脸检测器，3D CG 与真人用 SCRFD-10G | 公开 ReID 数据均为监控行人，与影视素材不匹配 |
| 人物检测 | 以画面显著程度替代剧情重要程度：清晰度加人脸框宽度占比达标者计为主体，其余按路人丢弃；门槛按画风分三档 | "谁算主体"缺乏可计算依据 |
| 镜头内跟踪 | 切镜时刻由 ffmpeg 场景检测给出，镜头内按 IoU 关联；宁多切不少切 | 漏一刀会把两个角色粘成一条轨迹并静默丢失一个身份 |
| 跨镜头去重 | 每条轨迹在线擂台选一张代表图，2D 用角色特征 CCIP、3D CG 与真人用 ArcFace，完全连接层次聚类 | 该环节为精度最薄弱处，见第三、四节 |
| 滑窗计数 | 窗口固定 30 秒（需求口径），窗口内不同身份数达标即输出片段 | — |
| 存在性判断 | 只输出"未发现"，不主张"不存在" | 存在性在逻辑上无法证明，该结论未被推翻 |
| 真值 | 素材文件名即人工标注，逐镜头人脸数与全片不同角色数两项 | 自建数据集不可行，改为极小规模标注 |

原说明中"公开数据不可用""自建数据不可行""存在性无法证明"三条未被消解，本项目分别以极小规模文件名标注和弱化输出形态予以规避；"模型不跨风格泛化"与"谁算主体"两条以画风路由和显著度替代做了工程近似。

## 二、当前效果（9 段 30 秒素材，出厂配置）

表 2　逐视频结果

| 测试视频 | 画风 | 真值角色数 | 预测角色数 | 轨迹数 | 召回 | F1 | 画风判定 |
|---|---|---|---|---|---|---|---|
| JOJO的奇妙冒险_635s | 2D | 9 | 7 | 74 | 0.78 | 0.88 | 正确 |
| 擅长捉弄的高木同学_325s | 2D | 7 | 9 | 76 | 1.00 | 0.88 | 正确 |
| 新世纪福音战士_1050s | 2D | 6 | 6 | 24 | 1.00 | 1.00 | 正确 |
| 新世纪福音战士_1270s | 2D | 7 | 7 | 26 | 1.00 | 1.00 | 正确 |
| 魔女之旅_760s | 2D | 9 | 9 | 35 | 1.00 | 1.00 | 正确 |
| 凡人修仙传_1040s | 3D CG | 6 | 6 | 24 | 1.00 | 1.00 | 正确 |
| 凡人修仙传_695s | 3D CG | 7 | 8 | 50 | 1.00 | 0.93 | 正确 |
| 爱情神话_1525s | 真人 | 4 | 4 | 8 | 1.00 | 1.00 | 正确 |
| 爱情神话_925s | 真人 | 11 | 14 | 59 | 1.00 | 0.88 | 正确 |

注：真值取自文件名中的"跨镜头身份去重后期望"；召回按数量口径计算并截断于 1.0，故须与 F1 同读。

表 3　汇总指标

| 指标 | 数值 |
|---|---|
| 宏平均召回 / 微平均召回 | 0.98 / 0.97 |
| 宏平均 F1 | 0.95 |
| 分风格 F1（2D / 3D CG / 真人） | 0.95 / 0.97 / 0.94 |
| 角色数与真值完全一致 | 5 / 9 段 |
| 画风三分类正确 | 9 / 9 段 |

剩余误差为两个相反方向：JOJO 7/9 与高木同学 9/7，同一个 2D 阈值一端合并过度、另一端拆分过度；爱情神话_925s 14/11，街景路人未被尺寸门槛滤净。逐次运行存在约一个角色的抖动，量级低于真值噪声本身。上述参数均对 9 个样本拟合而得，其中非 2D 各仅两段，更换素材须重新标定。

## 三、片长尺度上的失效

将同一套参数用于 17 段完整片源（共 7.3 小时）：

表 4　完整片源实测

| 观察项 | 结果 |
|---|---|
| 稳定性与吞吐 | 一趟跑完无崩溃、无内存问题；2D 约 4 倍实时，真人与 3D CG 约 8 倍 |
| 合格窗口占比 | 676 / 884 个不重叠窗口合格，即 76% 的片长被判为合格片段 |
| 极端样本 | 单集 46 / 47 个窗口合格；仅双人剧为 19 / 48 |
| 身份过拆 | 单集 54 个角色，白发主角一人占 5 个簇，拆分边界是暗光、模糊与侧脸，不是身份 |
| 画风路由 | 真人整片证据比 0.77 被判为 3D CG（其 30 秒切片为 0.29，判定正确），尺寸门槛因此用错一档 |

机制在于完全连接层次聚类对离群样本零容忍：788 条轨迹的 1431 个簇对中，有 106 对的簇间中位距离已低于阈值，仅因极少数离群图使最大距离越界而被拒绝合并。轨迹越多越易撞上离群图，故该失效随片长恶化，在 30 秒素材上不出现。单调调高阈值无解，同一次运行中会同时出现"仍在拆"与"已合错"。

一并需要指出：现有真值测不出该问题。9 段各 30 秒的样本中几乎每条轨迹都对应一个真值身份，任何过滤都只会掉分，故第四节所述的"有害"结论存在评估盲区。

## 四、正脸判定不能提升效果的原因

由于身份特征模型均以正面人物图训练，团队提出以正脸判定抑制非正脸样本，以改善跨镜头去重的合并率。该假设经两代判据、两种用法实测后不成立。

判据本身准确：对真人素材 59 张代表图逐张目视核对，得分 0.9 以上全为正脸、0.0 全为侧脸，中间段为四分之三侧面，单调性成立；2D 支路单调性同样成立，仅噪声更大。因此负面结果不能归因于判据失准。

表 5　用法一，代表图加权（各画风最优 F1）

| 画风支路 | 权重 0（默认） | 权重 0.5 | 权重 1.0 |
|---|---|---|---|
| 2D 动画 | 0.95 | 0.94 | 0.94 |
| 3D CG | 0.97 | 0.97 | 0.97 |
| 真人实拍 | 0.94 | 0.92 | 0.92 |

差异落在逐次运行的抖动量级内，即结果为中性；完整片源上方向相反，24 分钟 2D 素材的角色数由 54 升至 57，耗时增加 27%。

表 6　用法二，正脸硬门槛（各画风最优 F1）

| 画风支路 | 门槛 0（默认） | 门槛 0.3 | 门槛 0.5 |
|---|---|---|---|
| 2D 动画 | 0.95 | 0.92 | 0.85 |
| 3D CG | 0.97 | 0.92 | 0.95 |
| 真人实拍 | 0.94 | 0.50 | 0.45 |

注：后两列在权重 1.0 基础上叠加门槛；门槛越严结果越差，趋势单调。判定最准的真人支路跌幅最大。

表 7　两种用法失效的原因

| 用法 | 失效原因 |
|---|---|
| 代表图加权 | 离群样本类型不匹配。阻断簇间合并的代表图主要是暗光、模糊与遮挡画面而非侧脸；完整片源上仍有 34% 的轨迹找不出任何一帧正脸分高于 0.3 的画面，正脸分对该类样本不具区分度 |
| 代表图加权 | 作用位置不对。完全连接聚类取决于簇间最大距离，换一张更正的代表图无法降低离群对的距离上界。同期实测的"每轨迹多张代表图取中位数"与"两段式分位数合并"同样无效，三者原因一致 |
| 正脸硬门槛 | 误差不对称。真值只统计该时段出现了几个不同角色，而某角色可能全程仅以侧脸出镜，丢弃即直接抹掉一个真值身份，属不可恢复的错误；保留它至多使特征略有污染，仍有被正确合并的机会 |

结论是正脸判定不应出现在任何会丢弃轨迹的位置。相关实现与单元测试予以保留，三档画风配置的正脸权重与硬门槛均默认关闭。需保留的一处存疑：完整片源上真人支路会聚出整簇的背身、耳部与近乎全黑样本，硬门槛恰可清除该类样本，但现有评估集天然惩罚一切过滤，其收益无从体现，须待片长尺度的人工标注后重新评估。

## 五、结论

在半分钟尺度上，简化实现已能给出与人工标注基本一致的角色数：宏平均召回 0.98、宏平均 F1 0.95、画风路由全对。这一结果建立在 9 个样本的拟合之上，且并未推翻原说明的判断——公开数据缺失、自建数据不可行、存在性不可证明三条依旧成立，本项目是以极小规模标注与弱化输出形态绕开，而非解决。

在片长尺度上，同一套参数即告失效，制约因素集中在跨镜头身份去重一环：身份特征对画质与姿态敏感，全片一次性聚类使离群样本随片长累积。已实测无效的改进包括正脸加权、多代表图取中位数与分位数合并，三者均在改善聚类的鲁棒性，而非特征本身。后续工作应先修正整片上判错的画风路由，补充代表图质量门槛，并将出镜时长门槛由按轨迹改为按角色统计；同时须先取得片长尺度的人工标注，否则任何改进的效果都无从判定。

## 六、之后打算优化的方向

按优先级排列如下，前两项为本阶段主要动作。

表 8　后续方向

| 优先级 | 方向 | 针对第三节的哪项失效 | 做法 | 主要风险 |
|---|---|---|---|---|
| 1 | 聚类范围由全片改为单个 30 秒窗口 | 身份过拆；离群样本随片长累积 | 每个窗口内独立聚类，只在窗口内判定唯一身份数，不再维护全片一致的角色编号 | 相邻窗口结果彼此独立，同一角色跨窗口编号不一致；窗口内轨迹数少，阈值更敏感 |
| 2 | 补充若干 1080p 真人素材并标注 | 真人支路参数仅由两段同源素材拟合 | 扩充真人测试集并按现有文件名口径标注，重跑阈值网格重新标定真人档参数 | 标注成本；新素材可能推翻现有真人档取值 |
| 3 | 修正整片上的画风路由 | 真人整片被判为 3D CG，尺寸门槛用错一档 | 更换判据，不再依赖两个检测器的置信度之和之比 | 现判据在整片上真人与 3D CG 的间隔已消失，需重找判据，非调阈值可解 |
| 4 | 补代表图质量门槛 | 背身、耳部、近乎全黑的整簇无效样本 | 真人对齐图的黑边占比可直接由仿射矩阵算出，另加整体亮度下限，均无需回看像素 | 属过滤操作，在现有评估集上必然掉分，收益须待片长标注才可验证 |
| 5 | 最短出镜时长改为按角色累计 | 轨迹被切镜强制打断，单条轨迹时长不等于角色出镜时长 | 在聚类之后、选段之前，按簇累计出镜时长过滤 | 已实测该口径清掉的是无效簇，治不了过拆；且同为过滤操作，风险同上 |
| 6 | 合格门槛按片长重定或改为相对口径 | 76% 的片长被判为合格片段 | 现门槛为绝对值 4 人，改为随素材角色密度取相对值 | 需求口径为绝对人数 X，相对口径与需求不一致，仅可作为诊断指标 |

第一项之所以排在首位，是因为需求本身只问"某个 30 秒窗口内是否有 X 个不同角色"，并不需要全片一致的身份编号。全片一次性聚类求解的是一个更难且非必需的问题，而第三节的失效正随轨迹总数恶化——窗口内的轨迹数比整片低一到两个数量级，恰好回到第二节中该方法成立的规模。代价是放弃跨窗口的身份一致性，这一点在现有需求下不构成损失。

第二项与其余各项的关系是：第四、五两项均为过滤操作，在现有 9 段各 30 秒的评估集上必然掉分，其真实收益无从判定。因此扩充测试集不只是补真人样本，也是让后续任何改进变得可评估的前提。
